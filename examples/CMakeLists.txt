# maïs examples
#
# Every sample is a standalone host that embeds Python through maïs and owns
# its own loop. Built only when BUILD_EXAMPLES is enabled at the top level.

add_subdirectory(standalone_host)
