name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
            operating-system: linux
            runner: ubuntu-latest
            archive: tar.gz
          - name: macos
            operating-system: macos
            runner: macos-latest
            archive: tar.gz
          - name: windows
            operating-system: windows
            runner: windows-latest
            archive: zip
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v6.0.2

      - name: Install Linux dependencies
        if: matrix.operating-system == 'linux'
        run: bash scripts/install_ubuntu_dependencies.sh

      - name: Configure CMake
        # pybind11 owns Python discovery and picks the interpreter first on
        # PATH.
        run: >
          cmake
          -B ${{ github.workspace }}/build
          -S ${{ github.workspace }}
          -DBUILD_SHARED_LIBS=OFF
          -DBUILD_TESTING=OFF
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_INSTALL_PREFIX=${{ github.workspace }}/install

      - name: Build
        run: >
          cmake
          --build ${{ github.workspace }}/build
          --config Release

      - name: Install
        run: >
          cmake
          --install ${{ github.workspace }}/build
          --config Release

      - name: Package artifacts (Linux/macOS)
        if: matrix.archive == 'tar.gz'
        run: >
          tar -czf
          ${{ github.workspace }}/mais-${{ matrix.name }}.tar.gz
          -C ${{ github.workspace }}/install .

      - name: Package artifacts (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: >
          Compress-Archive
          -Path "${{ github.workspace }}\install\*"
          -DestinationPath "${{ github.workspace }}\mais-${{ matrix.name }}.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.2.1
        with:
          files: ${{ github.workspace }}/mais-${{ matrix.name }}.${{ matrix.archive }}
          generate_release_notes: true
